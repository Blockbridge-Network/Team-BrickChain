"use client";

const PropertyMap = ({ filters }: { filters: any }) => {
  return (
    <div className="bg-gradient-to-br from-gray-900/50 to-gray-800/50 rounded-xl p-6 border border-gray-800 h-[600px] flex items-center justify-center">
      <p className="text-gray-400">Map view coming soon...</p>
    </div>
  );
};

export default PropertyMap;
